# mkdir $"($nu.cache-dir)"

# (
#   starship init nu
#   | save --force  $"($nu.cache-dir)/starship.nu"
# )

# (
#   zoxide init nushell --cmd cd --hook pwd
#   | save --force $"($nu.cache-dir)/zoxide.nu"
# )

# (
#   carapace _carapace nushell
#   | save --force $"($nu.cache-dir)/carapace.nu"
# )
